'use client'
import {useMemo,useState} from 'react'
import {ArrowRight,BookOpen,BrainCircuit,Check,ChevronDown,ChevronRight,Copy,FolderHeart,Menu,Search,Sparkles,Star,Sun,Target, WandSparkles, X, Zap} from 'lucide-react'
import {prompts,categories,LIBRARY_TARGET,Prompt} from '@/lib/prompts'

const PAGE_SIZE=18

export default function Home(){
 const [query,setQuery]=useState(''); const [cat,setCat]=useState('All'); const [difficulty,setDifficulty]=useState('All'); const [copied,setCopied]=useState<number|null>(null); const [saved,setSaved]=useState<number[]>([]); const [open,setOpen]=useState<number|null>(null); const [mobile,setMobile]=useState(false); const [light,setLight]=useState(false); const [visible,setVisible]=useState(PAGE_SIZE); const [improve,setImprove]=useState(false); const [wizard,setWizard]=useState(0); const [rating,setRating]=useState<Record<number,number>>({});
 const filtered=useMemo(()=>prompts.filter(p=>{const text=`${p.title} ${p.description} ${p.category} ${p.industry} ${(p.tags||[]).join(' ')}`.toLowerCase();return(cat==='All'||p.category===cat)&&(difficulty==='All'||p.difficulty===difficulty)&&text.includes(query.toLowerCase())}),[query,cat,difficulty]);
 const shown=filtered.slice(0,visible); const selected=open?prompts.find(p=>p.id===open):null;
 const copy=(id:number,text:string)=>{navigator.clipboard?.writeText(text);setCopied(id);setTimeout(()=>setCopied(null),1600)}
 const toggleSave=(id:number)=>setSaved(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);
 return <main className={light?'light':''}>
  <nav className="glass nav"><div className="container nav-inner"><a href="#top" className="brand"><span className="brand-mark">P</span><span>PromptForge <i>Business</i></span></a><div className="desktop nav-links"><a href="#library">Library</a><a href="#learn">Learn</a><a href="#websites">Websites</a><a href="#pricing">Pricing</a></div><div className="nav-actions"><button className="btn btn-ghost" onClick={()=>setLight(!light)} aria-label="Toggle light and dark mode"><Sun size={16}/></button><a className="btn btn-primary desktop" href="#library">Explore prompts <ArrowRight size={15}/></a><button className="btn btn-ghost mobile" onClick={()=>setMobile(!mobile)} aria-label="Open navigation">{mobile?<X size={19}/>:<Menu size={19}/>}</button></div></div>{mobile&&<div className="mobile mobile-menu">{['Library','Learn','Websites','Pricing'].map(x=><a key={x} className="btn btn-ghost" href={'#'+x.toLowerCase()} onClick={()=>setMobile(false)}>{x}</a>)}</div>}</nav>

  <section id="top" className="hero grid-bg"><div className="container hero-inner"><div className="tag"><Sparkles size={14}/> THE DAILY AI WORKBENCH</div><h1 className="display">10,000+ Elite Prompts.<br/><span>Create stunning results for anything.</span></h1><p className="hero-copy">The definitive prompt library for people doing serious work with AI — business, creative, technical, strategic, and everything in between.</p><div className="hero-actions"><a className="btn btn-primary btn-lg" href="#library">Browse 10,000+ prompts <ArrowRight size={17}/></a><a className="btn btn-ghost btn-lg" href="#websites">Order your website</a></div><div className="proof-row"><span><Check size={14}/> 180 curated seed prompts</span><span><Check size={14}/> 30+ disciplines</span><span><Check size={14}/> Copy, save, improve</span><span><Check size={14}/> Built for 10,000+</span></div></div></section>

  <section className="container stats-wrap"><div className="card stats"><Stat icon={<Zap/>} n="10K+" t="library capacity"/><Stat icon={<Target/>} n="180" t="curated seeds"/><Stat icon={<BrainCircuit/>} n="30+" t="professional disciplines"/><Stat icon={<Star/>} n="4" t="learning paths"/></div></section>

  <section id="library" className="container section"><div className="section-head"><div><div className="tag">Prompt library</div><h2 className="display">Your unfair AI advantage.</h2><p className="muted">Search deeply. Filter precisely. Copy instantly. Improve until the output is exceptional.</p></div><div className="search-wrap"><Search size={18}/><input value={query} onChange={e=>{setQuery(e.target.value);setVisible(PAGE_SIZE)}} placeholder="Search 10,000+ prompts..." aria-label="Search prompts"/></div></div>
   <div className="filterbar"><div className="chips">{['All',...categories].map(c=><button key={c} className={'chip '+(cat===c?'active':'')} onClick={()=>{setCat(c);setVisible(PAGE_SIZE)}}>{c}</button>)}</div><label className="select"><span className="sr">Difficulty</span><select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>All</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select><ChevronDown size={15}/></label></div>
   <div className="library-meta"><span>{filtered.length} curated prompts matched</span><span>Architecture target: {LIBRARY_TARGET.toLocaleString()}+</span></div>
   <div className="prompt-grid">{shown.map(p=><PromptCard key={p.id} p={p} copied={copied===p.id} saved={saved.includes(p.id)} onCopy={()=>copy(p.id,p.prompt)} onSave={()=>toggleSave(p.id)} onOpen={()=>setOpen(p.id)} onImprove={()=>{setOpen(p.id);setImprove(true)}}/>)}</div>
   {shown.length<filtered.length&&<div className="load"><button className="btn btn-ghost" onClick={()=>setVisible(v=>v+PAGE_SIZE)}>Load more <ChevronRight size={15}/></button></div>}
   {filtered.length===0&&<div className="empty card"><Search size={22}/><b>No prompt matches that search.</b><span className="muted">Try a broader phrase, another category, or clear the filters.</span></div>}
  </section>

  <section id="learn" className="band"><div className="container section"><div className="two-col"><div><div className="tag"><BookOpen size={14}/> LEARNING HUB</div><h2 className="display">Master the craft,<br/>not just the syntax.</h2><p className="muted lead">Structured paths turn prompting from trial-and-error into a repeatable professional skill.</p></div><div className="path-list">{[['01','Prompt Engineering Mastery','8 lessons · foundations → advanced'],['02','Daily Prompt Habits','7 lessons · workflows that stick'],['03','Industry-specific Mastery','12 lessons · role-based systems'],['04','Stunning Visual & Written Output','10 lessons · craft + iteration']].map(x=><button className="card path" key={x[0]}><span className="path-no">{x[0]}</span><span><b>{x[1]}</b><small>{x[2]}</small></span><ChevronRight/></button>)}</div></div></div></section>

  <section id="websites" className="container section"><div className="website card glow"><div className="website-copy"><div className="tag"><BrainCircuit size={14}/> CUSTOM BUILD STUDIO</div><h2 className="display">Need more than prompts?<br/><span>We build the website.</span></h2><p className="muted lead">A strategy-led, conversion-focused build for your business — from discovery to polished launch.</p><div className="package-row"><span>Starter</span><span>Growth</span><span>Premium</span><span>Enterprise</span></div><button className="btn btn-primary btn-lg" onClick={()=>setWizard(1)}>Start your project <ArrowRight size={16}/></button></div><div className="process"><b>01 · Discover</b><p>Goals, audience, offer, competitors.</p><b>02 · Design</b><p>Structure, visual direction, prototype.</p><b>03 · Build</b><p>Fast, responsive, accessible engineering.</p><b>04 · Launch</b><p>QA, analytics, SEO and handover.</p></div></div></section>

  <section id="pricing" className="container section pricing"><div className="center"><div className="tag">PRICING</div><h2 className="display">Start free. Go deeper when useful.</h2><p className="muted">A large free library, then professional workflows for serious users and teams.</p></div><div className="price-grid">{[['Free','$0','Curated free library','Browse, copy, save locally'],['Pro','$12/mo','Full prompt access','Collections + advanced workflows'],['Growth','$49/mo','Team learning','Shared sets + analytics'],['Custom','Talk to us','Website strategy + build','Dedicated project team']].map((p,i)=><div className={'card price '+(i===1?'featured':'')} key={p[0]}><span className="muted">{p[0]}</span><strong className="display">{p[1]}</strong><b>{p[2]}</b><p className="muted">{p[3]}</p><a className={'btn '+(i===1?'btn-primary':'btn-ghost')} href={i===3?'#websites':'#library'}>{i===3?'Enquire':i===0?'Explore':'Start free'}</a></div>)}</div></section>

  <footer><div className="container footer"><span className="brand"><span className="brand-mark">P</span>PromptForge <i>Business</i></span><span className="muted">Built for people doing real work with AI.</span><span className="muted">© 2026 PromptForge</span></div></footer>

  {selected&&<div className="modal-backdrop" onClick={()=>{setOpen(null);setImprove(false)}}><div className="modal card" onClick={e=>e.stopPropagation()}><div className="modal-head"><div><span className="tag">{improve?'Improve / Expand':'Prompt detail'}</span><h2 className="display">{selected.title}</h2></div><button className="btn btn-ghost" onClick={()=>{setOpen(null);setImprove(false)}} aria-label="Close"><X size={17}/></button></div><div className="modal-tags"><span className="chip active">{selected.category}</span><span className="chip">{selected.difficulty}</span>{selected.tags?.slice(1,3).map(t=><span className="chip" key={t}>{t}</span>)}</div><p className="muted">{selected.description}</p><label className="prompt-label">MASTER PROMPT</label><pre>{selected.prompt}</pre><div className="example-grid"><div><label>Example input</label><p>{selected.exampleInput}</p></div><div><label>Example output</label><p>{selected.exampleOutput}</p></div></div><div className="tips"><b>Expert tips</b>{selected.tips?.map(t=><span key={t}>• {t}</span>)}</div><div className="modal-actions"><button className="btn btn-primary" onClick={()=>copy(selected.id,selected.prompt)}>{copied===selected.id?<><Check size={15}/> Copied</>:<><Copy size={15}/> Copy prompt</>}</button><button className="btn btn-ghost" onClick={()=>setImprove(!improve)}><WandSparkles size={15}/> {improve?'Use original':'Improve / Expand'}</button><div className="rating" aria-label="Rate prompt quality">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(r=>({...r,[selected.id]:n}))} className={n<=(rating[selected.id]||0)?'rated':''}><Star size={16} fill="currentColor"/></button>)}</div></div></div></div>}
  {wizard>0&&<div className="modal-backdrop" onClick={()=>setWizard(0)}><div className="wizard card" onClick={e=>e.stopPropagation()}><div className="modal-head"><div><span className="tag">WEBSITE STUDIO · STEP {wizard} OF 3</span><h2 className="display">Tell us what you're building.</h2></div><button className="btn btn-ghost" onClick={()=>setWizard(0)}><X/></button></div><div className="progress"><span style={{width:`${wizard*33.33}%`}}/></div>{wizard===1&&<div className="wizard-grid"><Field label="Business / brand" placeholder="e.g. Biloo"/><Field label="What do you sell?" placeholder="Product, service, platform..."/><Field label="Primary goal" placeholder="Leads, sales, bookings, credibility..."/><Field label="Target audience" placeholder="Who should this website serve?"/></div>}{wizard===2&&<div className="wizard-grid"><Field label="Pages needed" placeholder="Home, About, Services, Shop..."/><Field label="Visual direction" placeholder="Describe the feeling or references"/><Field label="Timeline" placeholder="When should it launch?"/><Field label="Budget range" placeholder="Starter / Growth / Premium / Enterprise"/></div>}{wizard===3&&<div className="wizard-done"><Check size={34}/><h3 className="display">Brief ready to send.</h3><p className="muted">We have the structure needed to scope your project. Connect the form to your preferred CRM/email endpoint before production launch.</p><a className="btn btn-primary" href="mailto:hello@promptforge.business?subject=PromptForge%20Website%20Project">Send project brief <ArrowRight size={15}/></a></div>}<div className="wizard-actions">{wizard>1&&<button className="btn btn-ghost" onClick={()=>setWizard(wizard-1)}>Back</button>}{wizard<3?<button className="btn btn-primary" onClick={()=>setWizard(wizard+1)}>Continue <ArrowRight size={15}/></button>:<button className="btn btn-ghost" onClick={()=>setWizard(0)}>Done</button>}</div></div></div>}

  <style jsx>{` .mobile{display:none}.container{width:min(1180px,calc(100% - 32px));margin:auto}.nav{position:sticky;top:12px;z-index:30;width:min(1180px,calc(100% - 22px));margin:12px auto 0;border-radius:18px}.nav-inner{height:64px;display:flex;align-items:center;justify-content:space-between;gap:20px}.brand{display:flex;align-items:center;gap:9px;font-weight:750;letter-spacing:-.02em}.brand i{font-style:normal;color:var(--muted);font-weight:550}.brand-mark{display:grid;place-items:center;width:30px;height:30px;border-radius:9px;background:var(--accent);color:#0b0d0f}.nav-links{display:flex;gap:28px;font-size:14px}.nav-actions{display:flex;align-items:center;gap:8px}.mobile-menu{padding:0 16px 16px;gap:8px;flex-direction:column}.hero{padding:110px 0 78px;overflow:hidden}.hero-inner{text-align:center;max-width:1050px}.hero h1{font-size:clamp(48px,8vw,92px);line-height:.96;letter-spacing:-.065em;margin:24px 0}.hero h1 span{color:var(--accent)}.hero-copy{max-width:720px;margin:0 auto;color:var(--muted);font-size:19px;line-height:1.65}.hero-actions,.proof-row{display:flex;justify-content:center;gap:10px;flex-wrap:wrap}.hero-actions{margin-top:30px}.proof-row{margin-top:28px;color:var(--muted);font-size:13px;gap:18px}.proof-row span{display:flex;align-items:center;gap:5px}.proof-row svg{color:var(--accent)}.btn-lg{padding:12px 17px}.stats-wrap{padding:22px 0 76px}.stats{display:grid;grid-template-columns:repeat(4,1fr);padding:13px}.section{padding:72px 0 92px}.section-head{display:flex;align-items:end;justify-content:space-between;gap:28px;margin-bottom:24px}.section h2{font-size:46px;letter-spacing:-.045em;margin:12px 0 6px}.search-wrap{width:min(420px,100%);position:relative}.search-wrap svg{position:absolute;left:14px;top:14px}.search-wrap input{width:100%;height:48px;padding:0 14px 0 42px;border:1px solid var(--line);border-radius:13px;background:var(--surface);color:var(--text);outline:none}.filterbar{display:flex;gap:12px;align-items:center;justify-content:space-between;margin-bottom:10px}.chips{display:flex;gap:7px;overflow:auto;padding:5px 0 12px}.chip{border:1px solid var(--line);background:var(--surface);color:var(--muted);padding:8px 11px;border-radius:999px;white-space:nowrap;font:inherit;font-size:12px;cursor:pointer}.chip.active{background:var(--accent);border-color:var(--accent);color:#0a0c0d}.select{position:relative;display:flex;align-items:center;gap:4px}.select select{appearance:none;padding:10px 29px 10px 12px;border:1px solid var(--line);background:var(--surface);color:var(--text);border-radius:10px}.select svg{position:absolute;right:9px;pointer-events:none}.library-meta{display:flex;justify-content:space-between;color:var(--muted);font-size:12px;margin:5px 0 14px}.prompt-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:13px}.prompt-card{padding:19px;min-height:295px;display:flex;flex-direction:column}.prompt-title{font-size:20px;line-height:1.1;margin:16px 0 8px}.prompt-description{font-size:13px;line-height:1.55;margin:0}.prompt-tags{display:flex;gap:5px;flex-wrap:wrap;margin-top:13px}.prompt-actions{display:flex;gap:7px;margin-top:auto;padding-top:17px}.prompt-actions .btn{padding:9px 11px;font-size:12px}.load,.center{text-align:center}.load{padding-top:25px}.empty{display:grid;place-items:center;gap:8px;padding:60px}.band{background:var(--band);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.two-col{display:grid;grid-template-columns:1fr 1.2fr;gap:70px;align-items:center}.lead{font-size:17px;line-height:1.7}.path-list{display:grid;gap:10px}.path{text-align:left;padding:17px;display:flex;align-items:center;gap:14px}.path-no{font-family:'Space Grotesk',sans-serif;color:var(--accent);font-weight:700}.path span:nth-child(2){flex:1}.path small{display:block;color:var(--muted);margin-top:4px}.website{padding:clamp(28px,6vw,65px);display:grid;grid-template-columns:1.35fr .65fr;gap:50px;background:radial-gradient(circle at 85% 20%,rgba(216,255,79,.09),transparent 32%),var(--surface)}.website h2{font-size:clamp(38px,5vw,62px);line-height:1.02}.website h2 span{color:var(--accent)}.package-row{display:flex;gap:7px;flex-wrap:wrap;margin:24px 0}.package-row span{border:1px solid var(--line);padding:8px 10px;border-radius:9px;font-size:12px}.process{border-left:1px solid var(--line);padding-left:28px;display:grid;align-content:center}.process b{margin-top:13px}.process p{color:var(--muted);margin:4px 0 4px}.price-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:32px}.price{padding:23px;display:flex;flex-direction:column;min-height:240px}.price strong{font-size:32px;margin:14px 0}.price p{min-height:44px}.price .btn{margin-top:auto}.price.featured{border-color:rgba(216,255,79,.45);box-shadow:0 0 50px rgba(216,255,79,.05)}footer{border-top:1px solid var(--line)}.footer{padding:30px 0;display:flex;justify-content:space-between;gap:14px;flex-wrap:wrap}.modal-backdrop{position:fixed;inset:0;z-index:60;background:rgba(0,0,0,.72);backdrop-filter:blur(10px);display:grid;place-items:center;padding:18px}.modal{width:min(820px,100%);max-height:92vh;overflow:auto;padding:28px}.modal-head{display:flex;justify-content:space-between;gap:20px}.modal-head h2{font-size:32px;margin:12px 0}.modal-tags{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:15px}.modal pre{white-space:pre-wrap;font:14px/1.7 'DM Sans',sans-serif;padding:18px;border-radius:13px;background:#090b0e;border:1px solid var(--line);color:#e8edf2}.prompt-label,.example-grid label{display:block;color:var(--muted);font-size:10px;font-weight:700;letter-spacing:.12em;margin:18px 0 8px}.example-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.example-grid>div,.tips{border:1px solid var(--line);border-radius:12px;padding:15px}.example-grid p{font-size:13px;line-height:1.55;margin:0}.tips{display:grid;gap:7px;margin-top:10px}.tips b{margin-bottom:2px}.tips span{font-size:13px;color:var(--muted)}.modal-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:16px}.rating{display:flex;margin-left:auto}.rating button{border:0;background:none;color:#555;cursor:pointer;padding:5px}.rating button.rated{color:var(--accent)}.wizard{width:min(720px,100%);padding:28px}.progress{height:4px;background:var(--line);border-radius:99px;margin:12px 0 25px;overflow:hidden}.progress span{display:block;height:100%;background:var(--accent)}.wizard-grid{display:grid;grid-template-columns:1fr 1fr;gap:13px}.field{display:grid;gap:7px}.field label{font-size:12px;font-weight:700}.field input{height:46px;padding:0 12px;border:1px solid var(--line);background:var(--surface);color:var(--text);border-radius:10px;outline:none}.wizard-done{text-align:center;padding:30px 0}.wizard-done>svg{color:var(--accent)}.wizard-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:24px}.sr{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0)}@media(max-width:900px){.prompt-grid{grid-template-columns:repeat(2,1fr)}.price-grid{grid-template-columns:1fr 1fr}.two-col,.website{grid-template-columns:1fr}.process{border-left:0;border-top:1px solid var(--line);padding:25px 0 0}.stats{grid-template-columns:1fr 1fr}.desktop{display:none!important}.mobile{display:flex}}@media(max-width:650px){.container{width:calc(100% - 22px)}.hero{padding-top:80px}.hero h1{font-size:clamp(43px,13vw,66px)}.hero-copy{font-size:16px}.section{padding:55px 0 70px}.section h2{font-size:36px}.section-head{display:grid}.prompt-grid{grid-template-columns:1fr}.filterbar{align-items:start;display:grid}.select{justify-self:start}.library-meta{display:grid;gap:5px}.price-grid{grid-template-columns:1fr}.example-grid,.wizard-grid{grid-template-columns:1fr}.stats{grid-template-columns:1fr}.footer{display:grid}.nav-inner{height:58px}.brand{font-size:14px}}`}</style>
 </main>
}
function PromptCard({p,copied,saved,onCopy,onSave,onOpen,onImprove}:{p:Prompt;copied:boolean;saved:boolean;onCopy:()=>void;onSave:()=>void;onOpen:()=>void;onImprove:()=>void}){return <article className="card prompt-card"><div style={{display:'flex',justifyContent:'space-between',gap:8}}><span className="chip active">{p.category}</span><span className="muted" style={{fontSize:11}}>{p.difficulty}</span></div><h3 className="display prompt-title">{p.title}</h3><p className="muted prompt-description">{p.description}</p><div className="prompt-tags">{(p.tags||[]).slice(0,3).map(t=><span className="chip" key={t}>{t}</span>)}</div><div className="prompt-actions"><button className="btn btn-primary" onClick={onCopy}>{copied?<><Check size={13}/> Copied</>:<><Copy size={13}/> Copy</>}</button><button className="btn btn-ghost" onClick={onSave}><FolderHeart size={13}/>{saved?'Saved':'Save'}</button><button className="btn btn-ghost" onClick={onOpen} aria-label={`View ${p.title}`}>View</button><button className="btn btn-ghost" onClick={onImprove} aria-label={`Improve ${p.title}`}><WandSparkles size={13}/></button></div></article>}
function Stat({icon,n,t}:{icon:React.ReactNode;n:string;t:string}){return <div style={{display:'flex',alignItems:'center',justifyContent:'center',gap:11,padding:12}}><span style={{color:'var(--accent)'}}>{icon}</span><div><b className="display" style={{fontSize:24}}>{n}</b><div className="muted" style={{fontSize:11}}>{t}</div></div></div>}
function Field({label,placeholder}:{label:string;placeholder:string}){return <div className="field"><label>{label}</label><input placeholder={placeholder}/></div>}
